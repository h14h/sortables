#!/bin/bash

# Get the next open item from todos.txt
next_open=$(grep -m 1 "^o " todos.txt)

if [ -n "$next_open" ]; then
    # Strip the 'o ' prefix and display stylistically
    todo_item="${next_open#o }"
    echo ""
    echo "📋 $todo_item"
else
    echo "No open items found in todos.txt"
fi
